A sample command-line application with an entrypoint in `bin/`, library code
in `lib/`, and example unit test in `test/`.



lets setup uno and make sure we want three directories where we want two css files there are one directory named npamp and othr is amp , and one sahred in both is shared in lib directory , let scan lib and make uno css work